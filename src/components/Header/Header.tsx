import React from 'react';
import PersonIcon from '@material-ui/icons/Person';

const Header = () => {
  return (
    <header>
      <nav>
        <PersonIcon />
      </nav>
    </header>
  );
};

export default Header;
